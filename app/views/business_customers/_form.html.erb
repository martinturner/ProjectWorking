<%= form_for(@business_customer) do |f| %>
  <% if @business_customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@business_customer.errors.count, "error") %> prohibited this business_customer from being saved:</h2>

      <ul>
      <% @business_customer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :business_id %><br>
    <%= f.text_field :business_id %>
  </div>
  <div class="field">
    <%= f.label :customer_id %><br>
    <%= f.text_field :customer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
